{
  "homepage": "http://krsteeve.github.io/EmojiWorkflow",
  "name": "emoji-workflow",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "gl-matrix": "^3.1.0",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-image-picker": "^1.1.1",
    "react-input-slider": "^5.1.3",
    "react-scripts": "3.2.0",
    "webgl-canvas": "^1.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^2.1.1"
  }
}
